# textual-xterm

A TypeScript library that integrates the [xterm.js](https://xtermjs.org/) terminal emulator with WebSocket communication. Designed as the client-side terminal component for [textual-serve](https://github.com/Textualize/textual-serve).

## Install

```bash
pnpm install
```

## Build

```bash
pnpm build
```
